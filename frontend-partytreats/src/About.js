const loadAbout = () => {
  // clearPage();
  // loadNav();
  //   fetch("http://localhost:3000/customers)
  //     .then((response) => response.json())
  //     .then((data) => {
  //       const index = document.getElementById("bg-te");
  //       data.map(
  //         (customer) =>
  //           (index.innerHTML += `
  //       <div>${customer.name} - ${customer.email}</div>
  //       <div>${customer.pastry_id} - ${customer.pastires}</div>
  //       `)
  //       );
  //     });
};
